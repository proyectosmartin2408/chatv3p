`render.com` // para crear el repositorio en render
`ingresar con el usuario de GitHub`  // ingresar con el usuario de GitHUB a Render
`escoger el servicio WEB Service`
`escoger el repositorio de GitHub que queremos desplegar`
`en start command poner gunicor main:app` //selccionar el branch sobre el que se creó el repositorio
`selccionar el plan free`
`darle deploy`


git config --global user.email "tu_email@ejemplo.com" // cambiar el correo del git